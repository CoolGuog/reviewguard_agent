# 核心依赖（必须）
torch>=2.0.0
transformers>=4.30.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Web Dashboard（必须）
flask>=3.0.0

# 爬虫模块（可选，仅 track_reviews.py 需要）
requests>=2.28.0
selenium>=4.15.0
undetected-chromedriver>=3.5.0

# LLM 摘要（可选，仅 reporter 需要）
openai>=1.0.0
